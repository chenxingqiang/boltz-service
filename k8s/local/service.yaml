apiVersion: v1
kind: Service
metadata:
  name: boltz-service
  namespace: boltz
  labels:
    app: boltz
spec:
  type: NodePort
  selector:
    app: boltz
  ports:
  - name: grpc
    port: 50051
    targetPort: grpc
    nodePort: 30051
